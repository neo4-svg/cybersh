# CYBER SH — Python Dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Core — AI engine (required)
# Also provides LlamaCache, used since v1.7 for in-RAM prompt/KV caching
# directly on the loaded .gguf model (faster repeat/RAG-augmented turns).
llama-cpp-python

# Web search — enables /web and /cvesearch commands (recommended)
ddgs

# Web agent — enables /fetch, /fetchauth, /fetchsites, /fetchforget (recommended)
tinydb

# Image generation — enables /image command (optional, ~4GB model download on first use)
diffusers
transformers
accelerate
safetensors

# Browser-based scraping — enables /fetch on JS-heavy sites like X.com, Instagram (optional)
# After install run: playwright install chromium
playwright
